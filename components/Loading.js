const Loading = () => {
    return (
        <div style={{ display: 'flex', justifyContent: 'center', height: '100vh', with: '100%', alignItems: 'center' }}>
            <div class="loader"></div>
        </div>
    )
}

export default Loading;
